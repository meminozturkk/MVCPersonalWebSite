@using PersonalWebSite.Models.Entity
@model List<TblInterest>

<div class="resume-section-content">
    <h2 class="mb-5">Hobilerim</h2>
    @foreach (var x in Model)
    {
    <p class="mb-0">@x.Explanation1</p>
    }
</div>
